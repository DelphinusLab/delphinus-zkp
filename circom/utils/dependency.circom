pragma circom 2.0.0;
// stub functions which will be implemented later

// used in bit.circom: bits_to_field, u32_from_2bits
function u32_from_bits(arr) {
  return 1;
}

// used in bit.circom: bits_to_field
function u32_to_field(N) {
  return 4;
}
